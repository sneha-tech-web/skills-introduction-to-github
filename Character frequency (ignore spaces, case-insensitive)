s = input("Enter a string: ").lower()
freq = {}

for ch in s:
    if ch != " ":
        if ch not in freq:
            freq[ch] = 1
        else:
            freq[ch] += 1

for ch in freq:
    print(ch, ":", freq[ch])
