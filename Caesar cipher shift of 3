text = input("Enter text: ")
result = ""

for ch in text:
    if ch.isalpha():
        if ch.isupper():
            result += chr((ord(ch) - 65 + 3) % 26 + 65)
        else:
            result += chr((ord(ch) - 97 + 3) % 26 + 97)
    else:
        result += ch

print("Encrypted:", result)
