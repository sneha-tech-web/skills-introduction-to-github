n = int(input("Enter number of rows: "))

for i in range(n, 0, -1):
    if i == n:
        print("*" * (2 * n - 1))
    elif i == 1:
        print(" " * (n - 1) + "*")
    else:
        print(" " * (n - i) + "*" + " " * (2 * i - 3) + "*")
