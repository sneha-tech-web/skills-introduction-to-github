import os

folder = "./files"
ext = ".txt"
for i, file in enumerate(os.listdir(folder), 1):
    if file.endswith(ext):
        new_name = f"document_{i}{ext}"
        os.rename(os.path.join(folder, file), os.path.join(folder, new_name))
