s = input("Enter a string: ")

s_clean = ""
for ch in s:
    if ch != " ":
        s_clean += ch.lower()

# reverse using while loop
rev = ""
i = len(s_clean) - 1
while i >= 0:
    rev += s_clean[i]
    i -= 1

if rev == s_clean:
    print("Palindrome")
else:
    print("Not a palindrome")
